@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@model StormEkspress.Models.UIModel.HomePageDto
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Restoran Başvuru Formu";
    var breadcrumbJson = ViewData["BreadcrumbJson"] as string;
    var pageTitle = ViewData["Title"] as string;
    var breadcrumbs = ViewData["Breadcrumbs"] as List<Breadcrumb>;
}
<div class="container-fluid page-header py-5" style="margin-bottom: 6rem;">
    <div class="container py-5">
        <h1 class="display-3 text-white mb-3 animated slideInDown">@ViewData["PageTitle"]</h1>
        <nav aria-label="breadcrumb animated slideInDown">
            <ol class="breadcrumb">
                @foreach (var breadcrumb in breadcrumbs)
                {
                    @if (breadcrumb.IsActive)
                    {
                        <li class="breadcrumb-item active" aria-current="page">@breadcrumb.Text</li>
                    }
                    else
                    {
                        <li class="breadcrumb-item"><a href="@breadcrumb.Url">@breadcrumb.Text</a></li>
                    }
                }
            </ol>
        </nav>
    </div>
</div>
<div class="container">
    <h1>Restoran Başvuru Formu</h1>
    <form method="post" asp-action="RestoranBasvuruFormu">
        <div class="form-group">
            <label>Restoran Adı</label>
            <input asp-for="RestaurantApplicationForm.RestaurantName" class="form-control" />
            <span asp-validation-for="RestaurantApplicationForm.RestaurantName" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label>E-mail</label>
            <input asp-for="RestaurantApplicationForm.Email" class="form-control" />
            <span asp-validation-for="RestaurantApplicationForm.Email" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label>Telefon Numarası</label>
            <input asp-for="RestaurantApplicationForm.Phone" class="form-control" />
            <span asp-validation-for="RestaurantApplicationForm.Phone" class="text-danger"></span>
        </div>
        <button type="submit" class="btn btn-primary">Gönder</button>
    </form>
</div>
